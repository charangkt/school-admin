<Window x:Class="SchoolAdmin.App.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="School Admin — Sign in"
        Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource AppBackgroundBrush}">
    <md:Card Margin="24" Padding="32" UniformCornerRadius="12">
        <StackPanel>
            <md:PackIcon Kind="School" Width="56" Height="56"
                         HorizontalAlignment="Center" Foreground="{StaticResource AccentBrush}" />
            <TextBlock Text="School Admin" FontSize="24" FontWeight="SemiBold"
                       HorizontalAlignment="Center" Margin="0,12,0,4" />
            <TextBlock Text="Sign in to continue" Foreground="{StaticResource MutedBrush}"
                       HorizontalAlignment="Center" Margin="0,0,0,24" />

            <TextBox x:Name="UsernameInput"
                     Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                     md:HintAssist.Hint="Username"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,0,0,16" />

            <PasswordBox x:Name="PasswordInput"
                         md:HintAssist.Hint="Password"
                         Style="{StaticResource MaterialDesignOutlinedPasswordBox}" />

            <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource ErrorBrush}"
                       TextWrapping="Wrap" Margin="0,12,0,0" />

            <Button Content="SIGN IN" Command="{Binding LoginCommand}" IsDefault="True"
                    Height="44" Margin="0,16,0,0" />
        </StackPanel>
    </md:Card>
</Window>
